eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false # 不注册
  server:
    enable-self-preservation: false # 上线率低不报警
spring:
  application:
    name: eureka
server:
  port: 8761